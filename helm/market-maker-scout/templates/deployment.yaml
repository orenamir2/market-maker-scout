apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {app: {{ .Release.Name }}}
  template:
    metadata:
      labels: {app: {{ .Release.Name }}}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports: [{containerPort: 8000}]
          env:
            - name: DATA_MODE
              value: {{ .Values.env.DATA_MODE | quote }}
            - name: SCAN_HISTORY_DIR
              value: {{ .Values.env.SCAN_HISTORY_DIR | quote }}
            - name: SCAN_WORKERS
              value: {{ .Values.env.SCAN_WORKERS | quote }}
            - name: MARKET_DATA_TIMEOUT_SECONDS
              value: {{ .Values.env.MARKET_DATA_TIMEOUT_SECONDS | quote }}
            - name: INDUSTRY_TIMEOUT_SECONDS
              value: {{ .Values.env.INDUSTRY_TIMEOUT_SECONDS | quote }}
            - name: DEFAULT_UNIVERSE_TIMEOUT_SECONDS
              value: {{ .Values.env.DEFAULT_UNIVERSE_TIMEOUT_SECONDS | quote }}
            - name: SCAN_TIMEZONE
              value: {{ .Values.env.SCAN_TIMEZONE | quote }}
          volumeMounts:
            - name: scan-history
              mountPath: /data
          readinessProbe: {httpGet: {path: /healthz, port: 8000}, initialDelaySeconds: 3, periodSeconds: 5}
          livenessProbe: {httpGet: {path: /healthz, port: 8000}, initialDelaySeconds: 10, periodSeconds: 10}
          resources: {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: scan-history
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ .Release.Name }}-scan-history
          {{- else }}
          emptyDir: {}
          {{- end }}
